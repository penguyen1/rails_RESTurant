<!-- Shows each food and order in a table row -->
<tr>
  <td><%= @foods[order[:id]-1].name %></td>
  <td>$<%= @foods[order[:id]-1].price %></td>
  <td><%= order.quantity %></td>
  <td>$<%= order.total %></td>
  <!-- Edit order quantity?? -->
  <td><%= link_to 'Delete', order_path(order),
          method: :delete, 
          data: {confirm: 'Are you sure you want to delete this order?'} %></td>
</tr>


<!-- How to update & display order total cost? (quantity*price) -->
<!-- How to update & display customer total cost? (sum of all order totals) -->